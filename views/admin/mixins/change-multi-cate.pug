mixin change-multi-cate(path)
                            form(
                                action= path
                                method="POST"
                                form-change-multi
                            )
                                .d-flex.align-items-start 
                                    .form-group 
                                        select(name="type" class="form-control")
                                            option(disabled selected) Chọn hành động
                                            option(value="active") Dang kinh doanh
                                            option(value="deactive") Ngừng kinh doanh
                                            option(value="delete-all") Xóa tat cả
                                            option(value="change-position") Thay đổi vị trí
                                    .form-group 
                                        input(
                                            class="form-control d-none"
                                            type="text"
                                            name="ids"
                                            value=""
                                    )
                                    button(type="submit" class="btn btn-primary") Hoàn thành