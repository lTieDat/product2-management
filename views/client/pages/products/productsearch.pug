extends ../../layout/default.pug 
include ../../mixins/box-head.pug
include ../../mixins/product-layout.pug
block main
    .container.my-3 
        .row 
            .col-12 
                if(message)
                    +box-head(message)
                else
                    +box-head('Danh sách sản phẩm')
        
        +product-gri(products)
                 